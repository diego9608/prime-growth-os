# ============================================================================
# ALEAR ATELIER ENVIRONMENT VARIABLES
# Copy this file to .env.local and fill in your values
# ============================================================================

# ----------------------------------------------------------------------------
# APP CONFIGURATION
# ----------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL=http://localhost:3000    # Your app URL (no trailing slash)

# ----------------------------------------------------------------------------
# FEATURE FLAGS (Public - visible in browser)
# ----------------------------------------------------------------------------
NEXT_PUBLIC_FEATURE_SGP=on                    # Strategic Growth Predictor (on/off)
NEXT_PUBLIC_FEATURE_AUTH=on                   # Authentication system (on/off)

# ----------------------------------------------------------------------------
# SGP CONFIGURATION
# ----------------------------------------------------------------------------
RESPONSE_CURVE_MODE=exponential               # hill | exponential
LLM_ROUTER_MODE=auto                         # auto | force_llm | force_rules
FORECAST_HORIZON_DAYS=90                     # Days to forecast ahead

# ----------------------------------------------------------------------------
# GOVERNANCE & COMPLIANCE
# ----------------------------------------------------------------------------
ENABLE_GOVERNANCE=true                       # Enable audit logging
AUDIT_RETENTION_DAYS=365                    # Days to keep audit logs
ANONYMIZE_EXPORTS=true                      # Remove PII from exports

# ----------------------------------------------------------------------------
# BUSINESS THRESHOLDS
# ----------------------------------------------------------------------------
CRITICAL_IMPACT_THRESHOLD_MX=100000         # MXN threshold for critical decisions
LOW_CONFIDENCE=0.5                          # Low confidence threshold
MIN_SENT_CONF=0.6                          # Minimum sentiment confidence

# ----------------------------------------------------------------------------
# LLM CONFIGURATION (Server-side only - DO NOT add NEXT_PUBLIC)
# ----------------------------------------------------------------------------
ANTHROPIC_API_KEY=                         # Required for LLM features

# ----------------------------------------------------------------------------
# SUPABASE AUTH (Required when NEXT_PUBLIC_FEATURE_AUTH=on)
# ----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=                  # Your Supabase project URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=             # Your Supabase anon key (safe for browser)
SUPABASE_SERVICE_ROLE_KEY=                 # Service role key (SECRET - server-side only, for admin ops)

# IMPORTANT: If NEXT_PUBLIC_FEATURE_AUTH=on and these are missing:
# - Auth pages will show an error message
# - Navigation will hide auth-related UI
# - App continues to work for non-auth features

# ----------------------------------------------------------------------------
# EMAIL PROVIDER (Choose one: resend | postmark | sendgrid)
# ----------------------------------------------------------------------------
EMAIL_PROVIDER=postmark                     # Email service to use
EMAIL_API_KEY=                              # API key for email service
EMAIL_FROM="Alear OS <no-reply@alear.io>"  # From address for emails
EMAIL_REPLY_TO="support@alear.io"          # Reply-to address

# ----------------------------------------------------------------------------
# OPTIONAL: MONITORING & ANALYTICS
# ----------------------------------------------------------------------------
# NEXT_PUBLIC_POSTHOG_KEY=                  # PostHog analytics
# SENTRY_DSN=                                # Error tracking
# VERCEL_ANALYTICS_ID=                      # Vercel Analytics

# ----------------------------------------------------------------------------
# DEPLOYMENT CONTEXT (Set by Netlify automatically)
# ----------------------------------------------------------------------------
# CONTEXT=                                   # production | deploy-preview | branch-deploy
# DEPLOY_PRIME_URL=                          # URL of the deploy